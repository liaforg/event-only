<h1> Events </h1>

<%= link_to 'Add Event', new_event_path %>

<% @events.each do |current_event| %>
  <%= render 'single_event', event: current_event %>
<% end %>

<p>
  <%= link_to 'New Event', new_event_path %>
</p>
